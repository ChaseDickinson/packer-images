{
  "variables": {
    "cpus": "1",
    "disk_size": "25600",
    "files": "files",
    "headless": "false",
    "home": "/home/{{user `ssh_username`}}/",
    "keep_registered": "false",
    "memory": "4096",
    "scripts_dir": "scripts",
    "skip_export": "false",
    "ssh_username": "ubuntu",
    "ssh_password": "ubuntu",
    "vm_name": "{{user `os_name`}}-{{user `os_type`}}-base"
  },
  "builders": [
    {
      "type": "hyperv-iso",
      "boot_command": [
        "{{user `boot_command`}}"
      ],
      "boot_wait": "2s",
      "communicator": "ssh",
      "cpus": "{{user `cpus`}}",
      "disk_block_size": "1",
      "disk_size": "{{user `disk_size`}}",
      "generation": "2",
      "headless": "{{user `headless`}}",
      "http_directory": "./{{ template_dir }}",
      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "keep_registered": "{{user `keep_registered`}}",
      "memory": "{{user `memory`}}",
      "output_directory": "..\\{{user `os_version`}}-base",
      "shutdown_command": "echo '{{user `ssh_password`}}' | sudo -S shutdown -P now",
      "skip_export": "{{user `skip_export`}}",
      "ssh_password": "{{user `ssh_password`}}",
      "ssh_timeout": "2h",
      "ssh_username": "{{user `ssh_username`}}",
      "switch_name": "Default Switch",
      "vm_name": "{{user `vm_name`}}"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "environment_vars": [
        "PASSWORD={{user `ssh_password`}}",
        "USERNAME={{user `ssh_username`}}"
      ],
      "expect_disconnect": true,
      "pause_before": "10s",
      "scripts": [
        "{{user `scripts_dir`}}/base.sh",
        "{{user `scripts_dir`}}/linux_cloud_tools.sh"
      ]
    }
  ],
  "post-processors": []
}