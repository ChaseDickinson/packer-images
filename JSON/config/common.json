{
  "base_output_directory": "{{user `output_directory`}}\\{{user `base_vm_name`}}",
  "base_vm_name": "{{user `os_name`}}.{{user `os_type`}}.base",
  "boot_wait": "2s",
  "communicator": "ssh",
  "cpus": "1",
  "disk_block_size": "1",
  "disk_size": "25600",
  "export_directory": "G:\\vms",
  "filename_prefix": "{{isotime \"06.01.02\"}}",
  "files_dir": "files",
  "full_output_directory": "{{user `output_directory`}}\\{{user `full_vm_name`}}",
  "full_vm_name": "{{user `vm_name`}}.full",
  "generation": "2",
  "headless": "true",
  "home": "/home/{{user `ssh_username`}}/",
  "http_directory": "{{template_dir}}/config/{{user `os_name`}}",
  "keep_registered": "false",
  "memory": "3072",
  "output_directory": "..\\",
  "scripts_dir": "scripts",
  "shutdown_command": "echo '{{user `ssh_password`}}' | sudo -S shutdown -P now",
  "skip_export": "true",
  "ssh_password": "ubuntu",
  "ssh_timeout": "2h",
  "ssh_username": "ubuntu",
  "switch_name": "Default Switch",
  "timestamp": "{{isotime \"0601021504\"}}",
  "vm_name": "{{user `timestamp`}}.{{user `os_name`}}.{{user `os_type`}}",
  "zip_directory": "{{user `export_directory`}}\\{{user `os_name`}}_{{user `os_type`}}\\{{user `filename_prefix`}}"
}