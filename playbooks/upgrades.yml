# upgrades.yml

---
  - name: Install latest OS updates
    hosts: localhost
    become: true

    tasks:
    - name: Update all packages to the latest version
      apt:
        update_cache: yes
        upgrade: dist
